# import Quadrant from 'quadrant.graphql'
# import Company from 'company.graphql'
# import Ring from 'ring.graphql'

type Technology {
  name: String!
  description: String
  isNew: Boolean
  published_at: Date
  inRadar: Boolean
  quadrant: Quadrant
  ring: Ring
  companies: [Company]
}
